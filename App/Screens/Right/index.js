// @flow
import * as React from "react";
import { Text } from "react-native";

import Layout from "NavigationTest/App/Components/Layout";

const Right = (): React.Node => (
  <Layout.Center>
    <Text>Right</Text>
  </Layout.Center>
);

export default Right;
