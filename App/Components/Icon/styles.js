// @flow
import styled from "styled-components";
import Feather from "react-native-vector-icons/Feather";

export const StyledIcon = styled(Feather).attrs(props => ({
  size: 24,
  color: "#3a3a3a",
}))``;
